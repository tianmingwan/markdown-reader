# 大文档打开卡顿 —— 诊断与修复方案

> 触发问题：打开 `公基所有错题/01_政治/00_政治_全部错题(1145题).md` 卡顿，其他文档同样存在。
> 结论：**不是磁盘/解析慢，而是「一次性把整篇 HTML 塞进 DOM」导致首次完整排版阻塞主线程 1~3.5 秒。**
> 状态：修复已实现并通过测试与实测验证；release 二进制已重新编译。

---

## 一、实测环境与方法

必须强调：本项目 Linux 桌面版用的是 **WebKitGTK**（`libwebkit2gtk-4.1`，2.52.6），
不是 Windows 的 WebView2/Chromium。两者在超大 DOM 上的表现差距极大，
**用 Chrome 测会严重低估问题**。因此所有关键数据都用真实引擎（WebKitGTK MiniBrowser，
与 app 同一内核）实测：

- 渲染管线：`mdreader-core` 的 `md::render`（Rust，pulldown-cmark）—— **不是瓶颈**（见下）
- 真实文档：政治 1145 题 / 法律 2086 题 / 公文 423 题 / 唯物辩证法
- 端到端验证：用真实 `dist/` 编译产物 + 桩掉 Tauri IPC，跑完整前端代码路径

---

## 二、根因定位（数据）

### 2.1 Rust 端渲染很快，可以排除

```
政治 1145题：source 2.0MB → HTML 2.39MB，Rust 耗时 39ms
法律 2086题：source 3.6MB → HTML 4.36MB，Rust 耗时 80ms
```

`md::render` 几十毫秒完成，语法高亮/图片改写都不是问题。
**瓶颈 100% 在前端 DOM 与排版。**

### 2.2 政治文档的体量

| 指标 | 数值 |
|---|---|
| markdown 源码 | 2.0 MB / 39691 行 / 829683 字 |
| 渲染后 HTML | **2.39 MB** |
| DOM 节点 | **41850** |
| `<details>`（每道题的答案+解析） | 1145 |
| `<p>` / `<li>` / `<strong>` | 9086 / 7781 / 13482 |
| 折叠状态下文档高度 | 866659 px |

### 2.3 耗时拆解（WebKitGTK 实测，政治 / 法律）

| 阶段 | 政治 | 法律 | 说明 |
|---|---|---|---|
| `DOMParser` 纯解析 | 101 ms | 145 ms | 可接受 |
| `content.innerHTML = html` 赋值 | 115 ms | 195 ms | 可接受 |
| **首次完整布局** | **1125 ms** | **1862 ms** | ← **真正的卡顿来源** |
| 二次布局（增量） | 0 ms | 0 ms | 布局本身没写坏 |
| **切换主题**（样式重算+布局） | 342+139 ms | 615+285 ms | 次要问题 |
| 展开单道题答案 | 35 ms | 63 ms | 单次卡顿 |
| 滚动（一次性插入后） | 16 ms/帧 | 22 ms/帧 | 60fps 正常 |
| 滚动（边补齐边滚，修复前） | 36.7 ms/帧、18 帧掉帧 | — | 需优化 |

关键点：**绘制首屏只需要布局前约 2000px，但浏览器被迫一次性排版整篇 866659px。**
这就是「点开文档后卡一下（甚至几秒）」的全部原因。

### 2.4 不是问题的项（已排除，避免白改）

| 怀疑项 | 实测结论 |
|---|---|
| Rust 渲染 / 语法高亮 | 39~80 ms，无问题 |
| KaTeX | `has_math` 仅按 `$` 判断；政治/法律文档 `$` 数量为 **0**，KaTeX 根本没加载 |
| Mermaid | 这两篇无 mermaid 块 |
| 滚动监听 | 全局只有 `ink.ts` 一个 `scroll` 监听，且内部有「无笔画直接 return」短路 |
| 图片 base64 | 走 `mdimg://` 自定义协议流式读文件，不是 base64 |
| 手写画布 | 尺寸锁定视口（`wrapContainer.clientWidth/Height`），没有整篇巨型画布 |
| 目录树扫描 / 4s SAF 轮询 | 桌面端路径不涉及；树只扫一次 |
| `user-select: text`、`position: relative` | 实测无显著影响 |

### 2.5 走过的弯路（重要，避免后人重踩）

| 尝试 | 结果 |
|---|---|
| `content-visibility: auto` 加在 `<details>` 上分块 | **滚动严重恶化**：16 ms/帧 → **87~213 ms/帧**。WebKit 在滚动路径上反复重算 `contain-intrinsic-size`。**禁用此方案** |
| `.preview-content { contain: content / layout style }` | 首次布局 **反而变慢**（1229 → 1589/1677 ms），无收益 |
| 去掉 `.preview { will-change: scroll-position }` | 无可测收益（保留原样，不在本次改动范围） |
| 主题切换只改 `:root` class | 无收益（成本来自 41850 节点的样式重算，非变量查找） |

---

## 三、修复方案：渐进式分块渲染

### 3.1 思路

不再「一次性 `innerHTML = 2.39MB`」，而是：

1. 把渲染好的 HTML 按 **标签边界** 切成 N 块（约 64KB/块 → 政治 19 块、法律 37 块）
2. **首块同步插入** → 首屏内容立刻可见
3. 其余块在后续**每一帧插一块**，每块插入后**主动读一次几何触发布局**
   —— 把排版成本摊到多个受控时机，而不是让用户滚动那一帧去承担
4. 全部插完后恢复滚动位置（中途测量 `scrollHeight` 会偏小，比例会算错）

### 3.2 为什么每块后必须主动布局

实测对比（政治，同一套真实前端产物）：

| 策略 | 首屏可见 | 补齐过程滚动 | 补齐完成后滚动 |
|---|---|---|---|
| 一次性插入（修复前） | **1911 ms** | — | 19 ms/帧 |
| 分块 + 不主动布局 | 235 ms | 36.7 ms/帧、**18 帧掉帧** | 16 ms/帧 |
| **分块 + 每块主动布局（采用）** | **235 ms** | **11.9 ms/帧、3 帧掉帧** | **16 ms/帧（60fps）** |

不主动布局时，排版被推迟到用户滚动的那一帧，滚动立刻掉到 100 ms+/帧。
主动布局把这份成本前置到「用户还没开始滚」的空档里。

### 3.3 为什么阈值是 400KB / 每块 64KB

- **阈值 400&nbsp;KB**：小于它的文档（绝大多数笔记）走原来的单次 `innerHTML`，行为与性能**完全不变**，零回归风险。
- **每块 64&nbsp;KB**：实测 30/60/64/80/120&nbsp;KB 五档，
  - 30KB：首屏 198 ms，但总耗时恶化到 2532 ms
  - 120KB：总耗时最好 1644 ms，但首屏退化到 272 ms
  - **64KB 是首屏与总耗时的最优折中**

### 3.4 改动清单

| 文件 | 改动 |
|---|---|
| `src/chunker.ts` | **新增**。`splitHtmlChunks()`：按标签边界无损切块；`PROGRESSIVE_THRESHOLD`、`CHUNK_TARGET` 常量 |
| `src/preview.ts` | `renderPreview()` 增加渐进式渲染分支；滚动位置改到「全部块插完后」恢复 |
| `src/main.ts` | `paintPreview()` 注释与顺序同步（避免在半插入状态下测量并恢复滚动） |
| `tests/chunker.test.mjs` | **新增** 6 项回归测试（见下） |
| `package.json` | `npm test` 纳入 chunker 测试 |

### 3.5 正确性保障

切块是本方案唯一有风险的地方（切坏 = 整篇排版错乱），因此：

- 切点只落在**标签起始位置**，绝不落在标签内部、属性值或 HTML 实体中间
- 新增 `tests/chunker.test.mjs`，6 项断言全部通过：
  - 切块 `join('')` 后与原串**逐字节相同**（无损）
  - 每块不以半个标签结尾
  - 不切断 HTML 实体（`&amp;`）与属性
  - 用 jsdom 重建后与原始 DOM **innerHTML 完全等价**，`<details>`/`<h3>` 数量一致
- 端到端实测：真实编译产物渲染后 `details` 数量、DOM 节点数、文档高度与修复前一致
- 已加渲染代号（`renderGeneration`）防重入：快速切文档时旧渲染立即放弃补齐，不会串内容
- 尊重 `prefers-reduced-motion`
- 手机列表视图（预览区 `display:none`）检测为不可测量，自动走原路径

---

## 四、修复效果

### 4.1 端到端实测（真实编译器产物 + 桩 Tauri IPC）

| 文档 | 首屏内容可见（修复前 → 修复后） | 提升 | DOM 节点（前后一致） |
|---|---|---|---|
| 政治 1145 题 | **1911 ms → 274 ms** | **≈7×** | 41968 / 41964 ✓ |
| 法律 2086 题 | **3510 ms → 282 ms** | **≈12×** | 75502 / 75495 ✓ |

### 4.2 隔离实测（WebKitGTK，政治，同一文档）

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 首屏可见 | 1317 ms | **235 ms** |
| 补齐过程滚动 | 36.7 ms/帧、18 帧掉帧 | **11.9 ms/帧、3 帧掉帧** |
| 补齐完成后滚动 | 16 ms/帧 | **16 ms/帧（60fps）** |
| 内容完整性 | 41850 节点 | 41854 节点（一致） |

### 4.3 行为上用户会看到什么

- 点开大文档：**约 0.25 秒**就出现正文，而不是先白屏/卡住 2~3.5 秒
- 之后 2~5 秒内文档在「边读边补齐」，期间滚动基本流畅（偶有轻微掉帧）
- 补齐完成后滚动稳定 60fps
- **400KB 以下的普通文档完全无变化**

---

## 五、验证与复现

```bash
# 类型检查
npx tsc --noEmit

# 前端测试（含新增的切块回归测试）
npm test

# Rust 侧（渲染管线）
npm run test:rust

# 构建并安装产物
npm run build
npm run tauri build -- --no-bundle
sudo install -m755 src-tauri/target/release/mdreader /usr/bin/mdreader
```

---

## 六、后续可选优化（本次未做，按收益排序）

| 优先级 | 项 | 现状 | 预期收益 | 风险 |
|---|---|---|---|---|
| P1 | **题目级虚拟滚动** | 2086 道题的 DOM 全在文档里 | 首屏 <100ms、节点数从 75k 降到 ~1k，彻底消除大文档卡顿 | 高：锚点跳转、查找、滚动位置恢复都要重做 |
| P2 | 主题切换重排 | 政治 342+139 ms、法律 615+285 ms | 用 `data-theme` 局部作用域替代全树选择器；或主题切换时加过渡遮罩 | 中 |
| P3 | 结果缓存 `(path, mtime) → html` | 每次切回标签都重新请求 Rust 渲染 | 二次打开省掉 39~80ms 渲染 + 一次 IPC | 低 |
| P4 | 修正 `has_math` 判定 | 只要出现 `$` 就加载 KaTeX（本次文档为 0，未触发） | 避免含单个 `$` 的文档白加载 261KB KaTeX | 低 |
| P5 | 多标签 HTML 内存 | 每个标签持有完整 HTML（法律单篇 4.36MB） | 标签数 >8 时 LRU 驱逐非活动标签 HTML | 低 |

---

## 七、附：本次新增/修改的文件

- `src/chunker.ts`（新增）
- `src/preview.ts`（渐进式渲染）
- `src/main.ts`（滚动恢复时机）
- `tests/chunker.test.mjs`（新增，6 项断言）
- `package.json`（`npm test` 纳入新测试）
- `PERF_FIX_PLAN.md`（本文件）
