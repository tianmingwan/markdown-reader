# 滑动性能方案 v2（待审阅）

> 背景：v1 修复（渐进式分块渲染）已上线，解决了「点开文档卡 2~3.5 秒」，
> 但**滑动仍不满意**。本文是重新诊断后的方案，含真实引擎实测数据与取舍，请审阅后再决定做到哪一步。

---

## 一、先纠正 v1 的一处测量错误

v1 报告里「稳态滚动 27.7ms/帧」这个数字**是错的**，来源是我测量时用 `article.scrollTop = x`
赋值滚动 —— 这会强制同步回流并阻塞合成器。改用**真实滚轮事件**重测：

| 滚动方式 | 法律(2086题，75k 节点) |
|---|---|
| `scrollTop` 赋值（我的错误测法） | 26.8 ms/帧 |
| **真实滚轮（用户实际行为）** | **16.1 ms/帧 = 60fps，0 掉帧** |

**结论：文档静态时，75k 节点在 WebKitGTK 下滚动是流畅的。** 之前的悲观数字作废。

---

## 二、滑动到底卡在哪（真实滚轮实测）

按时间点连续采样（政治 / 法律，修复后版本）：

| 时间点 | 政治 avg / 掉帧 | 法律 avg / 掉帧 |
|---|---|---|
| 首屏刚出现（**后台补齐进行中**） | **70.6 ms / 15 帧** | **144.4 ms / 26 帧** |
| +0.4s | 8.4 ms / 0 | 14.2 ms / 1 |
| +0.8s 之后 | 16 ms / 0 | 16 ms / 0 |

| 阶段 | 耗时 | 用户感受 |
|---|---|---|
| 插入首块 → **首帧真正画出** | **219~234 ms** | 一次短冻结 |
| 补齐剩余分块（后台） | 政治 2.4s / 法律 5.1s | **这期间滑动 70~144ms/帧 = 明显卡** |
| 补齐完成 | — | 16ms/帧，流畅 |

### 根因（结构性，不是参数能调的）

1. 首块插入后读一次几何 → 触发**全文档布局 219~234ms**（这一段用户无法滚动，属于「冻结」）
2. 后台每追加一块 → HTML 结构变化使整篇布局失效 → 若这次排版正好落在**用户滚动的那一帧**，该帧 70~144ms

我试过用门闸（滚动时暂停追加）来规避，**结论是治不好**：

| 方案 | 首屏 | 补齐期滚动 |
|---|---|---|
| v1 现状（64KB 块 + 每块强制布局） | 248 ms | 70 / 144 ms |
| 门闸：滚动时挂起追加 | 248 ms | 70 / 144 ms（首帧仍卡，因为在途的那次布局挡不住） |
| 门闸 + 不强制布局 | 270 ms | 143 ms（更差：布局集中到滚动帧） |
| 门闸 + 首块缩到 16KB | 164 ms | 45→131 ms，**且补齐延长到 11.6s** |

**只要「往文档里追加内容」，就一定会触发整篇重排。** 不减少 DOM 体量，这个抖动无法消除。

### 已再次确认无效的手段

| 手段 | 结果 |
|---|---|
| `content-visibility: auto` + `contain-intrinsic-size` | 首次布局 **2638ms（与不用完全一样）**，WebKit 对插入的 DOM 仍做全量布局 |
| `contain: content` / `layout style` | 首次布局 **1229 → 1589/1677ms（更慢）** |
| 去掉 `will-change` / 主题改 `:root` class | 无可测收益 |

---

## 三、真正能解决的方案：题目级虚拟滚动

### 3.1 原型实测结果（已跑通，非估算）

我按「题目块」切开文档，只把可见窗口放进 DOM，上下用 spacer 撑住高度：

| 指标 | 当前（v1） | **虚拟滚动原型** | 提升 |
|---|---|---|---|
| DOM 节点数 | 41850 / 75381 | **1206 / 1187** | **35~63×** |
| 首帧延迟 | 248 ms | **67 ms** | **3.7×** |
| 稳态滚动（真实滚轮） | 16.1 ms | **13.7 ms，0 掉帧** | 流畅 |
| 补齐期卡顿 | **70~144 ms/帧** | **不存在**（没有「补齐」过程） | 根治 |
| 滚动后节点数 | 75381 | 1187（恒定） | 内存占用极小 |

关键点：**没有「边读边补」这个阶段了** —— 首帧 67ms 出来就是最终可滚动状态。

### 3.2 各题高度可预测（解决「必须先测量全部高度」的问题）

原型里一次性测量全部题目高度要 2.1~3.4s，这是它唯一的代价。但我统计了题块高度分布：

| 文档 | 题块数 | 变异系数 | 采样 50 题估计总高误差 | 采样 100 题 |
|---|---|---|---|---|
| 政治 | 1145 | 0.19 | 2.1%（最坏 6.0%） | 1.5% |
| 法律 | 2086 | 0.22 | 2.6%（最坏 8.7%） | 1.8% |

题目高度非常均匀，因此**不需要预先测量全部**：

1. 同步测量前约 60 题（≈100ms）得到平均高度 → 用它估算其余题目的偏移
2. **立即渲染首窗**（首帧 ≈ 70~100ms）
3. 用户滚动经过的题块，用**真实上下文就地测量**并就地修正偏移
4. 修正时**锚定当前可见题块在屏幕上的位置**，所以视觉上不跳动（滚动条刻度会逐步变准）

---

## 四、必须正视的风险与代价（这是我没直接动手的原因）

虚拟滚动会动到很多既有功能，**每一项都要单独适配，否则会静默坏掉**：

| 功能 | 影响 | 适配成本 |
|---|---|---|
| 全文搜索命中定位 | 命中的题目可能不在 DOM 里 | 需「按索引跳转 → 先渲染该窗口 → 再高亮」 |
| `#锚点` / 片段跳转 | 目标不在 DOM，`scrollIntoView` 失效 | 需按块索引换算滚动位置 |
| 浏览器 Ctrl+F 查找 | 只能找到窗口内的内容 | **无法根治**（需自建查找或保留非虚拟模式） |
| 跨题文本选择 / 复制 | 跨窗口选择会断 | 中等 |
| 手写批注 `ink.ts` | 笔画坐标基于文档像素，spacer 会改变文档总高 | 需把坐标映射改为「题块索引 + 块内偏移」 |
| 预览直接编辑 `editor.ts` | `contenteditable` 只作用于窗口，序列化会丢内容 | **建议虚拟模式下禁用编辑**，或退出虚拟 |
| 滚动位置记忆（比例） | 总高是估算值，比例会漂 | 需改存「题块索引 + 块内比例」 |
| 「展开全部解析」类操作 | 全展开会让 DOM 暴涨 | 需限制或虚拟模式下禁用 |

我建议的收敛方式：**虚拟滚动只在「大文档 + 纯阅读态」开启**，一旦进入编辑 / 批注 / 展开全部，就自动退出虚拟模式。

---

## 五、建议的方案（分三阶段，风险递增，可随时停在任一阶段）

### 阶段 A（低风险，~30 行，立刻可上）
缩小首块并让首帧更快：首块 16KB → 首帧 248ms **降到 164ms**。
**但补齐期滑动依旧卡**（实测 45→131ms/帧），只治标。

### 阶段 B（推荐，中风险，收益最大）
**实现题目级虚拟滚动**，按第三节的原型 + 第四节的收敛策略：

- 新增 `src/virtual.ts`：块切分、偏移表、窗口渲染、锚定修正
- `preview.ts`：大文档走虚拟渲染，小文档走原路径（阈值不变，400KB）
- `preview.ts` 维护 `path → 块偏移表` 缓存（首次估算，滚动中精修）
- `search.ts` / `main.ts`：搜索命中与 `#锚点` 改为「按块索引跳转」
- `editor.ts` / `ink.ts`：进入编辑或批注时自动退出虚拟模式（回退到 v1 路径）

预期结果：首帧 ≈70~100ms、滑动恒定 60fps、DOM 从 75k 降到约 1.2k、内存大幅下降。
**代价**：浏览器原生 Ctrl+F 只能命中当前窗口（我会在方案里明确标注这个退化）。

### 阶段 C（可选，低风险，独立于 B）
主题切换优化（政治 342+139ms、法律 615+285ms）——与滑动无关，可单独做。

---

## 六、需要你拍板的三点

1. **是否接受「浏览器原生 Ctrl+F 只能找到当前可见题目」这个退化？**
   如果不接受，虚拟滚动就不适合，我们只能停在阶段 A（首帧优化，滑动仍有补齐期卡顿）。
2. **是否接受「虚拟模式下禁用预览直接编辑」**（退出虚拟即可正常编辑）？
3. 是否接受**先做「纯阅读态虚拟」的最小版本**给你试用，再补搜索/锚点适配？
   这样你能先用真机感受，而不是一次性押上全部改动。

---

## 七、附：本轮实测的复现方式

所有数据来自与 app 同一内核的 WebKitGTK（`libwebkit2gtk-4.1` 2.52.6）：
- `scrolldiag2.html` —— 分时间点滚动采样（真实 `WheelEvent`）
- `virt.html` —— 虚拟滚动原型
- 关键修正：必须用真实滚轮事件测滚动，`scrollTop` 赋值会给出误导性的差结果
